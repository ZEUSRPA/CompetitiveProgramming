#include <bits/stdc++.h>
#define ENDL '\n'
typedef long long int lli;
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    lli n;
    cin>>n;
    vector<lli> c(n);
    for(int i=0;i<n;i++){
        cin>>c[i];
    }
    sort(c.begin(),c.end());
    for(int i=0;i<n;i++){
        
    }
    return 0;
}