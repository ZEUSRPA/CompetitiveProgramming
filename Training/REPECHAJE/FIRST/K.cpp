#include <bits/stdc++.h>
#define ENDL '\n'
typedef long long int lli;
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n,m,aux,tot=0,con=0,pos=0;
    cin>>n>>m;
    unordered_map<int,unordered_set<int>> videos;
    for(int i=0;i<n;i++){
        cin>>aux;
        videos[aux].insert(i);
    }
    while(tot<m){
        if(cont())
    }
    
    return 0;
}